# OpenCV aruco demo

The Aruco package is part of the OpenCV contrib module, which must be installed separately from the core module. 
This repos shows how the Aruco module can be used from a C++ project.
